server:
  port: 8888

camunda:
  bpm:
    default-serialization-format: "application/json"
    admin-user:
      id: admin
      password: admin
spring:
  datasource:
    url: jdbc:h2:mem:camunda
  h2:
    console:
      enabled: true
      settings:
        web-allow-others: true

#logging:
#  level:
#    root: debug